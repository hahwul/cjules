# typed: strict
# frozen_string_literal: true

# This file is rendered by cjules's release pipeline (.github/workflows/publish-homebrew.yml).
# DO NOT EDIT by hand.
class Cjules < Formula
  desc "Power-user CLI for the Jules API"
  homepage "https://github.com/hahwul/cjules"
  version "__VERSION__"
  license "MIT"

  on_macos do
    # macOS binaries are dynamically linked; openssl@3 is the only non-system
    # dependency (enforced by an otool guard in release-binary.yml).
    depends_on "openssl@3"

    on_arm do
      url "https://github.com/hahwul/cjules/releases/download/v__VERSION__/cjules-v__VERSION__-osx-arm64"
      sha256 "__SHA_OSX_ARM64__"
    end
    on_intel do
      url "https://github.com/hahwul/cjules/releases/download/v__VERSION__/cjules-v__VERSION__-osx-x86_64"
      sha256 "__SHA_OSX_X86_64__"
    end
  end

  on_linux do
    # Linux release binaries are statically linked (musl), so they are self-contained.
    on_arm do
      url "https://github.com/hahwul/cjules/releases/download/v__VERSION__/cjules-v__VERSION__-linux-arm64"
      sha256 "__SHA_LINUX_ARM64__"
    end
    on_intel do
      url "https://github.com/hahwul/cjules/releases/download/v__VERSION__/cjules-v__VERSION__-linux-x86_64"
      sha256 "__SHA_LINUX_X86_64__"
    end
  end

  def install
    bin.install Dir["cjules-v__VERSION__-*"].reject { |f| f.end_with?(".sha256") }.first => "cjules"
  end

  test do
    system "#{bin}/cjules", "version"
  end
end
